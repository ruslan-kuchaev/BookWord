@page
@model wood.Pages.RegisterModel
@{
    ViewData["Title"] = "Регистрация";
}

<div class="container-fluid py-5">
    <div class="row align-items-center min-vh-100">
        <div class="col-lg-5 py-5">
            <div class="registration-form mx-auto">
                <div class="text-center mb-4">
                    <h1 class="h3 fw-bold">Добро пожаловать!</h1>
                    <p class="text-muted">
                        Уже есть аккаунт? 
                        <a asp-page="/Login" class="text-decoration-none fw-bold text-primary">Войти</a>
                    </p>
                </div>
                
                <form method="post">
                    <div asp-validation-summary="All" class="alert alert-danger" role="alert"></div>
                    
                    <div class="mb-3">
                        <label asp-for="Input.Email" class="form-label fw-medium">Электронная почта</label>
                        <input asp-for="Input.Email" class="form-control form-control-lg rounded-3" 
                               placeholder="your@email.com" autocomplete="email">
                        <span asp-validation-for="Input.Email" class="text-danger small"></span>
                    </div>
                    
                    <div class="mb-3">
                        <label asp-for="Input.Password" class="form-label fw-medium">Пароль</label>
                        <div class="position-relative">
                            <input asp-for="Input.Password" class="form-control form-control-lg rounded-3" 
                                   id="password" type="password"
                                   placeholder="Создайте пароль" autocomplete="new-password">
                            <button type="button" class="btn btn-link position-absolute end-0 top-50 translate-middle-y text-decoration-none" 
                                    onclick="togglePassword()" style="z-index: 5;">
                                <i class="fas fa-eye" id="toggleIcon"></i>
                            </button>
                        </div>
                        <span asp-validation-for="Input.Password" class="text-danger small"></span>
                        
                        <div class="mt-3 small">
                            <div class="text-muted mb-2">Требования к паролю:</div>
                            <ul class="list-unstyled mb-0">
                                <li class="mb-1 requirement" id="length">
                                    <i class="fas fa-circle me-2 fa-xs text-muted"></i>Не менее 8 символов
                                </li>
                                <li class="mb-1 requirement" id="uppercase">
                                    <i class="fas fa-circle me-2 fa-xs text-muted"></i>Одна заглавная буква
                                </li>
                                <li class="mb-1 requirement" id="lowercase">
                                    <i class="fas fa-circle me-2 fa-xs text-muted"></i>Одна строчная буква
                                </li>
                                <li class="mb-1 requirement" id="special">
                                    <i class="fas fa-circle me-2 fa-xs text-muted"></i>Один специальный символ ($!%*?&)
                                </li>
                                <li class="mb-1 requirement" id="number">
                                    <i class="fas fa-circle me-2 fa-xs text-muted"></i>Одна цифра
                                </li>
                            </ul>
                        </div>
                    </div>
                    
                    <div class="mb-4">
                        <div class="form-check">
                            <input asp-for="Input.EmailConsent" class="form-check-input" type="checkbox">
                            <label asp-for="Input.EmailConsent" class="form-check-label small text-muted">
                                Я хочу получать письма о новинках и специальных предложениях
                            </label>
                        </div>
                    </div>
                    
                    <div class="mb-4 text-center small text-muted">
                        Создавая аккаунт, вы соглашаетесь с 
                        <a href="#" class="text-decoration-none fw-bold">Условиями использования</a> и 
                        <a href="#" class="text-decoration-none fw-bold">Политикой конфиденциальности</a>
                    </div>
                    
                    <button type="submit" class="btn btn-primary w-100 btn-lg rounded-3 fw-bold py-3 mb-3">
                        Зарегистрироваться
                    </button>
                </form>
            </div>
        </div>
        
        <div class="col-lg-6 d-none d-lg-flex align-items-center justify-content-center quotes-section">
            <div class="quotes-container">
                <div class="quote-item">
                    <i class="fas fa-quote-left quote-icon"></i>
                    <p class="quote-text">"Книги — это зеркала: в них отражается только то, что у тебя в душе."</p>
                    <p class="quote-author">— Карлос Руис Сафон</p>
                </div>
                <div class="quote-item">
                    <i class="fas fa-quote-left quote-icon"></i>
                    <p class="quote-text">"Чтение — вот лучшее учение. Следовать за мыслями великого человека — есть наука самая занимательная."</p>
                    <p class="quote-author">— А.С. Пушкин</p>
                </div>
                <div class="quote-item">
                    <i class="fas fa-quote-left quote-icon"></i>
                    <p class="quote-text">"Книга — это волшебница. Книга преобразила мир. В ней память человеческого рода."</p>
                    <p class="quote-author">— Н.А. Морозов</p>
                </div>
                <div class="quote-item">
                    <i class="fas fa-quote-left quote-icon"></i>
                    <p class="quote-text">"Хорошая книга — точно беседа с умным человеком."</p>
                    <p class="quote-author">— Л.Н. Толстой</p>
                </div>
                <div class="quote-item">
                    <i class="fas fa-quote-left quote-icon"></i>
                    <p class="quote-text">"Читать — это еще ничего не значит; что читать и как понимать читаемое — вот в чем главное дело."</p>
                    <p class="quote-author">— К.Д. Ушинский</p>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .registration-form {
        max-width: 600px;
        background: white;
        padding: 2.5rem;
        border-radius: 20px;
        box-shadow: 0 10px 40px rgba(0,0,0,.1);
        transition: box-shadow 0.3s ease;
    }
    
    .registration-form:hover {
        box-shadow: 0px 0px 29px -16px  rgba(0,0,0,.40);
    }
    
    .quotes-section {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        min-height: 90vh;
        -webkit-box-shadow: 0px 0px 24px 25px rgba(0, 0, 0, 0.2) inset;
        -moz-box-shadow: 0px 0px 24px 25px rgba(0, 0, 0, 0.2) inset;
        box-shadow: 0px 0px 24px 25px rgba(0, 0, 0, 0.2) inset;
        border-radius: 10px;
        position: relative;
        overflow: hidden;
    }
    
    .quotes-container {
        position: relative;
        height: 500px;
        width: 100%;
        max-width: 600px;
        overflow: hidden;
    }
    
    .quote-item {
        position: absolute;
        width: 100%;
        padding: 3rem;
        background: rgba(255, 255, 255, 0.95);
        border-radius: 20px;
        animation: slideUp 25s linear infinite;
        opacity: 0;
    }
    
    .quote-item:nth-child(1) {
        animation-delay: 0s;
    }
    
    .quote-item:nth-child(2) {
        animation-delay: 5s;
    }
    
    .quote-item:nth-child(3) {
        animation-delay: 10s;
    }
    
    .quote-item:nth-child(4) {
        animation-delay: 15s;
    }
    
    .quote-item:nth-child(5) {
        animation-delay: 20s;
    }
    
    .quote-icon {
        font-size: 3rem;
        color: #667eea;
        opacity: 0.3;
        margin-bottom: 1rem;
    }
    
    .quote-text {
        font-size: 1.5rem;
        font-weight: 500;
        color: #333;
        margin-bottom: 1.5rem;
        line-height: 1.6;
    }
    
    .quote-author {
        font-size: 1.1rem;
        color: #666;
        font-style: italic;
        text-align: right;
        margin: 0;
    }
    
    @@keyframes slideUp {
        0% {
            bottom: -100%;
            opacity: 0;
        }
        4% {
            bottom: 50%;
            transform: translateY(50%);
            opacity: 1;
        }
        20% {
            bottom: 50%;
            transform: translateY(50%);
            opacity: 1;
        }
        24% {
            bottom: 150%;
            opacity: 0;
        }
        100% {
            bottom: 150%;
            opacity: 0;
        }
    }
    
    .form-control:focus {
        box-shadow: 0 0 0 0.25rem rgba(102, 126, 234, 0.25);
        border-color: #667eea;
    }
    
    .btn-primary {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        border: none;
        transition: transform 0.2s ease, box-shadow 0.2s ease;
    }
    
    .btn-primary:hover {
        transform: translateY(-2px);
        box-shadow: 0 10px 25px rgba(102, 126, 234, 0.4);
        background: linear-gradient(135deg, #5568d3 0%, #6a3f8f 100%);
    }
    
    .btn-primary:active {
        transform: translateY(0);
    }
    
    .text-primary {
        color: #667eea !important;
    }
    
    .requirement.valid i {
        color: #198754 !important;
    }
    
    .requirement.invalid i {
        color: #dc3545 !important;
    }
    
    @@media (max-width: 991px) {
        .registration-form {
            padding: 2rem;
        }
    }
</style>

<script>
    function togglePassword() {
        const passwordInput = document.getElementById('password');
        const toggleIcon = document.getElementById('toggleIcon');
        
        if (passwordInput.type === 'password') {
            passwordInput.type = 'text';
            toggleIcon.className = 'fas fa-eye-slash';
        } else {
            passwordInput.type = 'password';
            toggleIcon.className = 'fas fa-eye';
        }
    }
    
    document.getElementById('password').addEventListener('input', function(e) {
        const password = e.target.value;
        
        updateRequirement('length', password.length >= 8);
        updateRequirement('uppercase', /[A-Z]/.test(password));
        updateRequirement('lowercase', /[a-z]/.test(password));
        updateRequirement('special', /[$!%*?&]/.test(password));
        updateRequirement('number', /[0-9]/.test(password));
    });
    
    function updateRequirement(id, isValid) {
        const element = document.getElementById(id);
        const icon = element.querySelector('i');
        
        if (isValid) {
            element.classList.add('valid');
            element.classList.remove('invalid');
            icon.className = 'fas fa-check-circle me-2 fa-xs text-success';
        } else {
            element.classList.add('invalid');
            element.classList.remove('valid');
            icon.className = 'fas fa-circle me-2 fa-xs text-muted';
        }
    }
</script>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
